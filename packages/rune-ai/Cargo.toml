[package]
name = "rune-ai"
version = "0.1.0"
edition = "2024"
description = "AI/ML model lifecycle governance — registry, versioning, training data governance, evaluation gates, deployment approval, bias/fairness monitoring policy, drift detection policy, and model retirement for the RUNE governance ecosystem"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha3 = "0.10"
hex = "0.4"
