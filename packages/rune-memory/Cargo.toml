[package]
name = "rune-memory"
version = "0.1.0"
edition = "2024"
description = "Memory governance, retention policies, retrieval governance, isolation boundaries, and memory audit for the RUNE governance ecosystem"
license = "MIT"

[dependencies]
rune-lang = { path = "../..", default-features = false }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha3 = "0.10"
hex = "0.4"
