[package]
name = "rune-permissions"
version = "0.1.0"
edition = "2024"
description = "Capability-based permission system for the RUNE governance ecosystem"
license = "MIT"

[dependencies]
rune-lang = { path = "../..", default-features = false }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
