[package]
name = "rune-data"
version = "0.1.0"
edition = "2024"
description = "Data pipeline governance — quality rules, classification, lineage, access governance, schema evolution, catalog, freshness monitoring"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha3 = "0.10"
hex = "0.4"
