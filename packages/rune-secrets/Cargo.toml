[package]
name = "rune-secrets"
version = "0.1.0"
edition = "2024"
description = "Sensitive data lifecycle management for the RUNE governance ecosystem"
license = "MIT"

[dependencies]
rune-lang = { path = "../..", default-features = false }
rune-permissions = { path = "../rune-permissions" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
hex = "0.4"
