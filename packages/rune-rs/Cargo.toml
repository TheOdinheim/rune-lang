[package]
name = "rune-rs"
version = "0.1.0"
edition = "2024"
description = "Rust integration for the RUNE governance-first policy engine"
license = "MIT"

[dependencies]
rune-lang = { path = "../..", features = ["runtime"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
